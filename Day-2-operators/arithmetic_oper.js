/* Task 1: Write a program to add two numbers and log the 
   result to the console. */
   
   let a = 5;
   let b = 7;
   console.log("a + b =", a+b);

/* Task 2: Write a program to subtract two numbers and log the 
   result to the console. */  

   let c = 9;
   let d = 4;
   console.log("c - d =", c-d);

/* Task 3: Write a program to multiply two numbers and log the 
   result to the console. */   

   let m = 13;
   let n = 3;
   console.log("m * n =", m*n);

/* Task 4: Write a program to devide two numbers and log the 
   result to the console. */   

   let q = 43;
   let r = 4;
   console.log("q / r =", q/r);

/* Task 5: Write a program to find the reminder is devided by another and log the 
   result to the console. */  
   
   let s = 5;
   let t = 2;
   console.log("s % t =", s%t);