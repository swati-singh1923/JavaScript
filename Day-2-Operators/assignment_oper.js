/* Task 1: Use the += operatorto add a number to a variable 
   and log the result to the console. */
   
   let age = 18;
   age += 1;
   console.log("age =", age);

/* Task 2: Use the -= operatorto add a number to a variable 
   and log the result to the console. */   

   let stud = 20;
   stud -= 1;
   console.log("stud", stud);

/* Task 3: Use the *= operatorto add a number to a variable 
   and log the result to the console. */   

   let x = 20;
   x *= 3; 
   console.log("x =", x);

/* Task 4: Use the /= operatorto add a number to a variable 
   and log the result to the console. */   

   let y = 120; 
   y /= 7;
   console.log("y =", y);

/* Task 5: Use the %= operatorto add a number to a variable 
   and log the result to the console. */   

   let m = 20;
   m %= 3;
   console.log("m =" ,m);